[build]
builder = "nixpacks"

[deploy]
startCommand = "python advanced_api_server.py"
healthcheckPath = "/health"
healthcheckTimeout = 300
restartPolicyType = "on_failure"

[environments.production]
variables = { 
  NODE_ENV = "production",
  PYTHONPATH = "/app",
  PORT = "8000"
}